:::info Version stamp

**Applies to:** `renvor` **0.0.0** · MSRV **1.94.0** · documentation set **v0.0.0**

This stamp is a single shared partial (`docs/_stamp.mdx`) imported by every prose page and
by the API reference. Prose and API documentation therefore describe the same contract at
the same version by construction — they cannot drift, because there is only one value
(FR-056).

:::
