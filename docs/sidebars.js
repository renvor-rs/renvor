// @ts-check

/**
 * Sidebar for the Renvor documentation set.
 *
 * Declared explicitly rather than autogenerated, so that adding a file does not silently
 * change the published navigation. A new page must be listed here to appear.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  docsSidebar: [
    'intro',
    {
      type: 'category',
      label: 'Project foundation',
      collapsed: false,
      items: ['support-policy', 'verification', 'governance'],
    },
    'cli',
    'api-reference',
  ],
};

export default sidebars;
