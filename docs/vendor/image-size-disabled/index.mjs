// ESM view of ./index.cjs. See fromFile.cjs for the rationale (T108).

import mod from './index.cjs';

export const {imageSize, imageSizeFromFile, setConcurrency, types, disableTypes} = mod;
export default mod.default;
