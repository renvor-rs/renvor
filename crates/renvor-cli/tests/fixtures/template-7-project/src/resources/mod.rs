//! Resources added by `renvor generate resource`: one module each.
//!
//! The two marker lines below are the only lines that command edits in this file, and the
//! matching pair in `src/routes.rs` is the only other edit it makes. Keep both pairs.

// renvor:resources:modules:begin
// renvor:resources:modules:end
