# Generated by renvor. This file records the choices generation ACTED ON.
#
# A choice appears here only if a generated file reflects it. A manifest recording an unhonoured
# choice would describe a project that was never generated.

[renvor]
generator_version = "0.0.0"
template_version = "3"

[project]
name = "legacy-v3"
target = "api"
transport = "rest"
local_domain = "legacy-v3.test"
container = true
local_https = "off"
example_domain = false
seed_data = false

[persistence]
# Recorded because generation ACTED on it: `src/persistence.rs` and `migrations/` exist.
database = "postgres"
orm = "sqlx"
driver_feature = "db-postgres"

