//! The `renvor` executable.
//!
//! Phase 003. Everything this binary prints — its command names, its flag names, its exit codes,
//! the split between `stdout` and `stderr`, and the shape of its JSON — is a **public contract**
//! from the first release that ships it. See `specs/003-interactive-cli/contracts/`.

fn main() {
    // T003: builds from the first commit, parses nothing yet.
}
