# The `[storage]` section for {{ name }}: objects under a directory this project ignores.
#
# The root must exist at Boot — the provider probes it — and is opened as a capability, so
# nothing the application does can leave it. `var/` is git-ignored.
root = "var/objects"
