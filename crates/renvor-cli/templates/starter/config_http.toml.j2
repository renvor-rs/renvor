# The `[http]` section for {{ name }}. Environment overrides: `RENVOR_HTTP_ADDRESS`,
# `RENVOR_HTTP_LOCAL_DOMAIN`. Nothing here is a secret.
address = "127.0.0.1:3000"
local_domain = "{{ local_domain }}"
