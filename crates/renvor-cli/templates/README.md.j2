# {{ name }}

Generated by renvor. Target: `{{ target }}`.

## Run

```
cargo run
```

## Local domain

`{{ local_domain }}`
{% if local_https == "requested" %}
Local HTTPS was **recorded as wanted**. Nothing has been issued and no trust store has been
modified — that is a later phase's work, and doing it silently is exactly what renvor will not do.
{% endif %}

## Serving HTTP

This project records `transport = "{{ transport }}"` in `renvor.toml`, and **does not yet depend on
the Renvor framework**.

That is deliberate, and it is stated rather than hidden: **no Renvor crate is published to
crates.io yet**. A generated `Cargo.toml` naming `renvor` as a dependency would not resolve, so this
project would not build — and a generator that emits a project which cannot be built has not
generated a project.

So `[dependencies]` is empty and `cargo build` works today. When the crates are published, add:

```toml
[dependencies]
renvor = { version = "<the published version>", features = ["transport-rest"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
```

and declare your routes against one registry, which is also what `renvor routes` inspects:

```rust
use renvor::{Method, RouteRegistry, Response};

let mut routes = RouteRegistry::new();
routes.get("/health", |_| async { Response::text("ok") })?;
```

Until then this file is the only place that describes the wiring, because describing it in code
that cannot compile would be worse.
{% if database %}
## Persistence

This project records `database = "{{ database }}"` and `orm = "{{ orm }}"` in `renvor.toml`, and
generation acted on both: `src/persistence.rs` holds the statements and the sort allowlist, and
`migrations/` holds a reversible `0001_create_item` pair.

For the reason above, `Cargo.toml` declares no driver yet. When the crates are published, add:

```toml
[dependencies]
renvor-sqlx = { version = "<the published version>", features = ["{{ driver_feature }}"] }
```

`{{ driver_feature }}` resolves the {{ database }} driver and **only** that driver — selecting one
database does not build the other.

### What is already correct in `src/persistence.rs`

- **Every value is bound, never interpolated.** The statements are constants; the data arrives
  beside them as parameters.
- **Every sortable column is on an allowlist.** A column name cannot be a bound parameter, so the
  only safe construction is one where every possible value is written in the source. An unknown
  sort field maps to no column and the sort is refused — not silently replaced with a default.
- **Ordering is total.** `id` is in the allowlist as a tiebreaker, because a cursor built on an
  order the database may vary between calls silently skips or repeats rows.

### Applying the migrations

Migrations are applied in **version order** — the numeric filename prefix — not filesystem order,
and each is checksummed when applied so an edited migration fails closed rather than diverging
silently. `0001_create_item` declares itself reversible by shipping a `.down.sql`; a migration
without one is irreversible, and a rollback of it is refused before anything is modified.
{% endif %}